<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Create note | noteblndr</title>
    <link href="/stylesheets/style.css" rel="stylesheet" type="text/css">
  </head>
  <body>
    <% if (user) {%>
      <%- include('header') %>
      <form action="delete" method="POST">
        <div class="m-auto flex min-h-full max-w-lg flex-col justify-center px-6 py-6 lg:py-12 lg:px-8">
          <div class="border-b border-gray-900/10 pb-12">
            <h2 class="text-2xl font-semibold text-gray-900">Are you shure you want to delete this note?</h2>
            <div class="mt-5 grid grid-cols-1 gap-x-6 gap-y-8 sm:grid-cols-6">
              <div class="sm:col-span-3 col-span-full">
                <label for="addressee" class="block text-sm font-medium leading-6 text-gray-900">The note will be deleted forever</label>
                <div class="mt-2 flex items-center justify-center gap-x-2">
                  <div>
                    <a href="./" value='' class="flex w-full justify-center border rounded-md bg-zinc-50 px-3 py-1.5 text-sm font-semibold leading-6 text-black shadow-sm hover:bg-gray-200 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-500">Cancel</a>
                  </div>
                  <div>
                    <button type="submit" class="flex w-full justify-center rounded-md bg-red-500 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-red-400 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-500">Yes, delete.</button>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </form>
    <% } else { %>
      <%- include('login-form') %>
    <%}%>
  </body>
</html>