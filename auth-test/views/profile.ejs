<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%- userData.username %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <%- include('header') %>
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
    <div class="flex items-center justify-center">
      <a href="" class="flex h-20 w-20 rounded-full bg-black focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-black">
        <img class="h-26 w-26 rounded-full p-1 invert" src="https://img.icons8.com/ios-glyphs/90/000000/user--v1.png" alt="">
      </a>
    </div>
    <div class="flex items-end justify-center gap-x-2 mt-3">
      <h1 class="font-bold text-center text-2xl">@<%- userData.username %></h1>
      <button class="flex justify-center rounded-md bg-zinc-950 px-3 py-0.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-pink-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-500">+ Añadir</button>
    </div>
    <h1 class="mt-10 w-full text-center text-md">Mirá las notas de <%- userData.username %>:</h1>
    <% Array.from(notes).forEach(function (note) { %>
      <%- include('note' , { note: note , formatDate: formatDate }) %>
    <% }); %>
  </div>
</body>
</html>